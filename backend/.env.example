# Backend server port (frontend proxies /api and /socket.io here).
PORT=4000

# Model / OpenRouter keys are read from the PIPELINE's .env at the repo root
# (../.env) — the same file the CLI uses. You do NOT need to duplicate the
# OPENAI_* keys here.
